packages:
  # unique_combination_of_columns — the composite grain check for the SCD2 dimensions
  # (business key + effectivedate). Everything else uses dbt's built-in generic tests.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
